# Architecture Notes

- Frontend: React + Vite
- Backend: Flask REST API
- Database: SQLite only
- Weather provider flow: WeatherAPI primary, Open-Meteo fallback
- Data stored locally through SQLAlchemy models
